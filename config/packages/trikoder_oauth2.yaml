trikoder_oauth2:

    authorization_server:
        private_key: /home/pi/work/symf/rest/restapi/config/keys/private.key                     # Change this
        private_key_passphrase: null                            # Passphrase of the private key, if any

        encryption_key: '%env(string:OAUTH2_ENCRYPTION_KEY)%'   # (Optional) Change this

    resource_server:
        public_key: /home/pi/work/symf/rest/restapi/config/keys/public.key                       # Change this

    persistence:
        doctrine: null
